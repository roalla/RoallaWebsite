# Vendored @roalla/design-system

Snapshot of `rcos/packages/design-system`. Do not hand-edit.

Refresh from a sibling RCOS checkout:

```bash
npm run design-system:sync
```

Digests are locked in `checksums.json` and enforced by `npm run product:validate`.
